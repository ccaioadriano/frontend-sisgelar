//TEMA PADRAO DA APLICAÇÃO

import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
    palette: {
        primary: {
            main: '#007BFF',
        },
        secondary: {
            main: '#f50057',
        },
    },
});

export default Theme;
