import { createTheme } from '@mui/material/styles';

const ThemeLogin = createTheme({
    palette: {
        primary: {
            main: '#007BFF',
        },
        secondary: {
            main: '#f50057',
        },
    },
});

export default ThemeLogin;
